import * as React from 'react';
import ReactMarkdown from 'react-markdown';
// import markdownContent from '../help.md';

export const HelpPage: React.FC = () => {
  return (
    <div className="app-section help">
      {/* <ReactMarkdown source={markdownContent} /> */}
    </div>
  );
}